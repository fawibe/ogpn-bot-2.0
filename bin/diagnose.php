<?php
declare(strict_types=1);require dirname(__DIR__).'/vendor/autoload.php';use Ogpn\Bot\{Bootstrap,Planner};$version=trim((string)@file_get_contents(dirname(__DIR__).'/VERSION'));echo 'Diagnostic CLI OGPN-BOT '.$version."\n";echo 'PHP '.PHP_VERSION."\n";foreach(['pdo_mysql','curl','json','mbstring','openssl','intl'] as $e)echo str_pad($e,16).(extension_loaded($e)?'OK':'ABSENT')."\n";try{$p=Bootstrap::pdo();echo 'MySQL          OK — '.$p->query('SELECT VERSION()')->fetchColumn()."\n";echo json_encode(Planner::metrics($p),JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE)."\n";}catch(Throwable $e){echo 'MySQL          ERREUR — '.$e->getMessage()."\n";exit(1);}
